//Библиотеки
#include <stdio.h>
#include <stdlib.h>
#include <time.h>



//Константы
#define M 10  //Строки матрицы
#define N 12  //Cтолбцы матрицы
#define L M/2  //Половина строк
#define K N/2  //Половина столбцов
#define T L*K*2  //Количество чисел в закрашенной области



//Начало функции поиска последнего положительного числа(Индекса) массива
int FindSrZnach(int a[M][N]) {  //Вспомогательная функция
    
    
    
    //Объявление переменных, указателей и т.п.
    int b[N];  //Объявление массива для передачи значений в главную функцию
    int i, j;  //Счёчтики для прохода по матрице
    int srznach = 0;  //Возвращаемое значение вспомогательной функции
    
    
    
    //Основной код(поиск среднего значения в закрашенной области)
    for (j = 0; j < N; j++) {  //Проход по строкам
        
        for (i = 0; i < M; i++) {  //Проход по столбцам
            
            srznach+=a[i][j];  //Прибавление к общей сумме всех чисел(Инверсия)
            
        }
        
        srznach = srznach / M;  //Получение среднего арифметического каждого столбца
        b[j] = srznach;  //Заполнение массива
        srznach = 0;  //Обнуление для последующего использования
        
    }

    
    
    //Возвращаемое значение функции
    return b[N];  //Вернуть массив
    
}


//Начало главной функции
int main() {  //Главная функция
    
    
    
    //Предустановки
    srand((unsigned int)time(NULL));  //Позволяет генерировать каждый раз "Новые" случайные числа
    
    
    
    //Объявление переменных, указателей и т.п.
    int a[M][N];  //Объявление массива
    int b[N];  //Объявление массива
    int i, j;  //Счёчтики для прохода по матрице

    
    
    //Основной код(Задание матрицы)
    printf("\n\n");  //Перенос для красоты вывода
    for (i = 0; i < M; i++) {  //Проход по строкам матрицы
        
        for (j = 0; j < N; j++) {  //Проход по столбцам
            
            a[i][j] = 1 + rand() % 9;  //Генерация случайного числа для "Отдельного элемента массива"
            printf("%5d", a[i][j]);  //Вывод каждого элемента матрицы
            
        }
        
        printf("\n\n");  //Двойной отступ для симметричности матрицы при выводе
        
    }
    
    
    
    //Вызов функции
    b[N] = FindSrZnach(a);  //Вызов вспомогательной функции + передать массив "а"
    
    
    
    //Основной код(Вывод в консоль среднего арифметического каждого столбца)
    printf("Массив: [ ");  //Визуальное начало массива
    for(i = 0; i < N; i++) {
        
        printf("%d ", b[i]);  //Вывод массива
        
    }
    printf("]\n\n");  //Визуальное начало массива + отступ для красоты вывода
    

    
    //Возвращаемое значение функции
    return 0;  //Вернуть 0
    
}

